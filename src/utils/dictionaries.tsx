import { Mail, Message, User } from "iconoir-react";
import { IconType } from "./types";

export const ICONS_DICTIONARY: Record<IconType, JSX.Element> = {
  user: <User />,
  mail: <Mail />,
  message: <Message />,
};
